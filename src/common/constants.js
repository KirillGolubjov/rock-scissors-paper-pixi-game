// Colors
 const WINNER_COLOR = '0x00ff00';
 const DRAW_COLOR = '0xa6a6a6';
 const START_COLOR = '0xffffff';

 // App
 const SCREEN_COLOR = '#000000';
 const SCREEN_WIDTH = 800;
 const SCREEN_HEIGHT = 600;

 // Scores
 const WIN = 'Wins!';
 const FINAL_SCORE = 3;
 const START_SCORE = 0;

 //TEXT
 const FONT_STYLE = 'Impact';
 const HUMAN = 'Human';
 const COMPUTER = 'Computer'
 const SELECT_HAND = `PRESS TO START >`
 const RESTART_GAME = 'RESTART_GAME';
 const RESTART = 'RESTART'
 const SCORE_TEXT = {
   fontSize: 75,
   fontFamily: FONT_STYLE,
  };
 const PLAYER_TEXT = {
   fontSize: 40,
   fontFamily: FONT_STYLE,
  };
  const RESTART_TEXT = {
   fontSize: 35,
   fontFamily: FONT_STYLE,
   fill: START_COLOR,
  };

export {
  WINNER_COLOR,
  DRAW_COLOR,
  START_COLOR,
  WIN,
  FINAL_SCORE,
  SCREEN_COLOR,
  SCREEN_WIDTH,
  SCREEN_HEIGHT,
  FONT_STYLE,
  HUMAN,
  COMPUTER,
  START_SCORE,
  SCORE_TEXT,
  PLAYER_TEXT,
  SELECT_HAND,
  RESTART,
  RESTART_TEXT,
  RESTART_GAME,
};